<div class="main-container-new">
  <% if user_signed_in? %>
    <h1 class="title-new">Nuevo Lead</h1>
    <%= render "form", lead: @lead %>
    <%= link_to "Volver", leads_path, class:"leadd-btn-new" %>
  <% else %>
    <%= render "form", lead: @lead %>
  <% end %>
</div>